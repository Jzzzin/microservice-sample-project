apply plugin: ProtoServicePlugin

dependencies {

    compile spring.retry
    compile cloud.config, cloud.configClient, cloud.security, cloud.oauth2
    compile boot.starterWeb, boot.actuator, boot.starterJpa
    compile db.postgres
    compile security.rsa

    testCompile boot.starterTest

}

